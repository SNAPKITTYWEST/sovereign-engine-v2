[package]
name    = "magma-safety"
version = "0.1.0"
edition = "2021"
description = "MAGMA Core electromagnetic safety and certificate library"

[dependencies]
sha2          = "0.10"
ed25519-dalek = { version = "2", features = ["rand_core"] }
rand_core     = { version = "0.6", features = ["getrandom"] }
