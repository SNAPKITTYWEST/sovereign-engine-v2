# `krull_dimension_definition` (C067)

Tier 6 — Krull dimension. *Generated from the crate source; regenerate after API changes.*

Krull dimension of a ring: supremum of lengths of chains of prime ideals.
dim(R) = sup{len(chain) : chain of primes in Spec(R)}

## Dependencies

- [C064 `spectrum_definition`](../C064/README.md)
- [C065 `spectrum_order`](../C065/README.md)
- [C066 `spectrum_chains`](../C066/README.md)

## Re-exports

- `spectrum_definition::Spectrum`

## Public API

| Item | Description |
|---|---|
| `struct KrullDim(pub usize)` | The Krull dimension of a ring |
| `fn KrullDim::from_spectrum(spec: &Spectrum) -> Self` | Krull dimension of a spectrum dim(R) = max(len(chain) - 1) for all prime chains or -1 if spectrum is empty |
| `fn KrullDim::dim(&self) -> usize` | Get numeric value |
| `fn KrullDim::is_zero_dimensional(&self) -> bool` | Check if ring is zero-dimensional |
| `fn KrullDim::is_one_dimensional(&self) -> bool` | Check if ring is one-dimensional |
| `fn KrullDim::is_catenary(spec: &Spectrum) -> bool` | Check if ring is catenary (all maximal chains have same length) |
| `fn KrullDim::maximal_chains(spec: &Spectrum) -> Vec<Vec<u64>>` | Get all maximal chains achieving this dimension |
| `fn KrullDim::height_bound_from_generators(num_generators: usize) -> Self` | Get height upper bound from number of generators For a ring generated by n elements, dim ≤ n |
| `fn KrullDim::localization_respects_dimension(dim_before: KrullDim, dim_after: KrullDim) -> bool` | Check if dimension is achievable by localization In a catenary ring, after localization, dimension can only decrease |
| `enum DimensionStrategy` | Dimension computation strategy |
| `struct DimensionCalculator` | Dimension calculator with strategy |
| `fn DimensionCalculator::new(strategy: DimensionStrategy) -> Self` | Create calculator with given strategy |
| `fn DimensionCalculator::compute(&self, spec: &Spectrum) -> KrullDim` | Compute dimension based on strategy |

## Tests

`cargo test -p krull_dimension_definition` runs 9 unit tests.
