# `ideal_interface` (C061)

Tier 6 — Krull dimension. *Generated from the crate source; regenerate after API changes.*

Interface for ideals in commutative rings.

## Dependencies

- [C001 `gap_tensor_core`](../C001/README.md)

## Public API

| Item | Description |
|---|---|
| `struct Ideal` | An ideal in a commutative ring |
| `fn Ideal::zero() -> Self` | Create the zero ideal |
| `fn Ideal::new(generators: Vec<u64>) -> Self` | Create an ideal from generators |
| `fn Ideal::principal(a: u64) -> Self` | Create a principal ideal (a) generated by single element |
| `fn Ideal::num_generators(&self) -> usize` | Number of generators |
| `fn Ideal::contains(&self, x: u64) -> bool` | Check if the ideal contains `x`. |
| `fn Ideal::same_ideal(&self, other: &Ideal) -> bool` | Do the two ideals have the same elements (same gcd)? |
| `fn Ideal::contains_ideal(&self, other: &Ideal) -> bool` | Check if this ideal contains another ideal |
| `fn Ideal::gcd(&self) -> u64` | The non-negative generator of the ideal: gcd of all generators, and 0 for the zero ideal. |
| `fn Ideal::intersection(&self, other: &Ideal) -> Option<Ideal>` | Intersection: `(a) ∩ (b) = (lcm(a, b))`; anything meets `(0)` in `(0)`. |
| `fn Ideal::sum(&self, other: &Ideal) -> Ideal` | Sum of two ideals (generators combined) |

## Tests

`cargo test -p ideal_interface` runs 8 unit tests.
