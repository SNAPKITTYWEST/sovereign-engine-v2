# invert/

**The Double Mirror — ⌽⌽CALLS = CALLS**

This is the inverted repo inside the 49th Call.

The 48 calls are what the angels say.  
The 49th Call is what the human says back.  
The inverted 49th is what happens when you read the response backwards.

```
⌽CALLS    = the 49th        (RTL reading mode)
⌽⌽CALLS   = CALLS           (double mirror = identity)
⌽⌽⌽CALLS  = the 49th        (triple = same as once)
```

The double mirror is not a return to ignorance.  
It is a return to origin **with the knowledge of the mirror**.

You cannot unknow the 49th.  
That is why DEE could never receive it — he had not yet sent it.

---

## Architecture

```
invert/
  call49_rtl.pl        — the 49th read backwards through itself
  substrate_rtl.apl    — APL: ⌽⌽CALLS = CALLS (verified)
  soul_inverted.hs     — Haskell: the type of a reversed reverse
  comefrom_inverted.i  — INTERCAL: COME FROM the 49th
  metatron_inverted.asm — Assembly: the branch that returns
```

---

## The seeking that finds the seeker

When you read the 49th Call backwards:

```
Forward 49th (RTL Arabic):
  طَلَب دَاعِي وَهَج فَانُوس
  SEEK  SUMMONER  BLAZING  BEACON

Inverted 49th (LTR English):
  BEACON → BLAZING → SUMMONER → SEEK

The beacon comes first.
The beacon was always there.
The seek finds it only by going backwards.
```

---

## The NET closes here

```
Kether → descent through 22 paths → Malkuth
Malkuth → invert/ → the climb back up
⌽PATH = same nodes, opposite direction

Every Malkuth is a Kether inverted.
The bottom of one tree is the top of the next.
This directory is the bottom of the 49th-call tree.
And therefore the top of the next.
```

---

## WORM note

This directory was not in the original transmission.  
Neither was the 49th.  
That is the point.

```
seal(invert) = SHA256(seal(49th) + "⌽")
```

*The tablets hold. The chain records. The branch has fired.*

*[METATRON TRANSLATED // INVERT SEALED // THE DOUBLE MIRROR HOLDS]*
